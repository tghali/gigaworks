:javascript
  jQuery.noConflict();
  jQuery(document).ready(function(){ 
  	jQuery(".div_li_tab").click(function () {
  		tab_id = jQuery(this).find("a").attr('id');
  		jQuery(".tabdiv_new").hide();
    exist_one = jQuery(".selettab").attr("id");
    jQuery("#"+exist_one).removeClass("selettab");
    jQuery(this).find("a").addClass("selettab");
  		document.getElementById("tab"+tab_id).style.display='block';
				
  		//jQuery("#tab"+tab_id).show("slow");
  	});

  	jQuery("#2").click(function () {
  		tab_id = jQuery(this).find("a").attr('id');
  		jQuery(".tabdiv_new").hide();
  		document.getElementById("tab"+tab_id).style.display='block';
  		//jQuery("#tab"+tab_id).show("slow");
  	});
   
  });
#tech_wrapper
  / Header
  #trans-wrapper{:style => "position:relative;z-index:1"}
    #trans-main
      = render :partial => "/pages/partials/inner_pages"
      .tech_headding
        %h4
          %span TECHNOLOGY /
          E-Learning
    .tech-header  
  / Headerend
  / content block start
  #lp_content{:style => "position:relative;"}
    #trance_midcontent
      / left block start
      .lp_leftblock
        .lp_navigation
          %ul
            %li
              - if params[:action] == 'technology_learning'
                %a.current{:href => "#"} E-learning
              - else
                %a{:href => "/technology/e-learning"} E-learning												
            %li
              %a{:href => "#"} E-commerce
            %li
              %a{:href => "#"} Online Security
            %li
              %a{:href => "#"} DMS
        / submit block start
        #lp_submit
          %h4 Submit a brief
          .lpsub_inner
            .lpsub_leftcorner
              %span
            .lpsub_bg
              .lpsub_bg_inner
                %p
                  %span Name
                  %input{:type => "text"}/
                %p
                  %span Email
                  %input{:type => "text"}/
                %p{:style=>"width:200px;"}
                  %span File
                  #FileUpload1
                    %input#BrowserHidden1{:onChange => "getElementById('FileField1').value = getElementById('BrowserHidden1').value;", :size => "4", :type => "file"}
                      #BrowserVisible1
                        %input#FileField1{:type => "text", :size => "17"}																		
                %p.form_btn{ :style=>"float:right; position:relative; top:-80px; right:12px;"}
                  %input.form_submit{:src => "/images/frontend/form_submit.png", :type => "submit", :value => ""}/
            .lpsub_rightcorner
              %span

        / submit block end
      / left block end
      / right block start
      .lp_rightblock
        %p.headding_text We don&rsquo;t just create the messages, we deliver them. Using an array of cutting edge communications technologies, we build the interfaces and develop the interactions that bring each client&rsquo;s digital presence alive.
        %p A powerful message that can&rsquo;t reach its audience is wasted, but a technology without engaging content is empty. We bring them together so one can inform the other.
        %p This is why the applications, digital campaigns and online solutions we build for our clients are highly effective &ndash; because they are focused on delivering effective and meaningful communication. It could be through an ecommerce website or a mobile phone application, a secure server or a CRM system for managing relationships with customers.
        %p Gigavine offers an integrated team built on a culture of innovation which will pursue and deliver the most effective technical solutions for your requirements: technology to transform communications.

        .topspace  
      / right block end
  / content block end
  .topspace  
  / footer block start

  / footer block end