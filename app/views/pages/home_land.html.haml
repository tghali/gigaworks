


#main-wrapper-home

  / Top holder
  = render :partial => "/pages/partials/login_links"

  / Top holder end
  = render :partial => "/pages/partials/home_header"

  / content block start
  =# render :partial => "/pages/partials/home_content"
  #main2{:style => "position:relative; z-index:1;"}
    / container for the slides
    = render :partial => "/pages/partials/slide_tabs"
    .clear
      %span
    / headding text start
    #corousol-wrapper
      .shadow_midbg
        %h4 Gigavine offers creative & technical services which cross borders & open minds
      .shadow_midbg
        %p
          As global markets grow, clear and open communication has never been more important to businesses and governments. We offer a seamless blend of creative solutions, cultural and linguistic translation and technical delivery which transform our clients &rsquo; communications. Our team of experts have helped organisations around the world to communicate and collaborate successfully across cultures and in multiple languages.
          %span We can do it for you too!
      %div
        / lastes news block start
        #latest-news
          = render :partial => "/pages/partials/latest_news"
        / lastes news block end
        / join network block start
        #join-the-network
          = render :partial => "/pages/partials/join_network"
        / join network block end
        / contact block start
        #contact-us
          = render :partial => "/pages/partials/contact_us"
        / contact block end
    / headding text end
    .clear
      %span
    / tabbing block start
    #tabbing
      .tabmainblock
        = render :partial => "/pages/partials/submit_block"

    / tabbing block end
  / content block end
  .clear  
  .clear  


