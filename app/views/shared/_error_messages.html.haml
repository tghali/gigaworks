- if target.errors.any?
  %div#validation-error
    %h3== #{pluralize(target.errors.count, "error")} prevented this record from being saved:
    %ul
      - target.errors.full_messages.each do |msg|
        %li= msg