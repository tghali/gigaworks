- content_for :parent do
  = table_for @contacts do |tabelle|  
    = tabelle.view "table" do |list|
      -# list.actions %w(verify suspend delete)
  - if can? :create, Contact
    = link_to 'Add Contact', new_contact_path